/**
 * Application constants
 */

export const COLORS = {
  RED: "#ff2d55",
  BLUE: "#2d8cff",
  WHITE: "#fff",
  BORDER_IDLE: "#fff",
};

export const ANIMATION = {
  PIXELATE_DURATION: 1800, // ms for a full sweep
  DEFAULT_PIXEL_SIZE: 16,
  TYPEWRITER_SPEED: 30, // ms per character
  TYPEWRITER_DELAY: 300, // ms before starting to type
};

export const STORAGE_KEYS = {
  SESSION_ID: 'woke_or_not_woke_session_id',
};

export const UI = {
  BACKGROUND: {
    GRADIENT: "linear-gradient(to bottom, #1b263b 0%, #0a1931 100%)",
    HEADER: "#0a1931",
  },
};
